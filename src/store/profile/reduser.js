import {ADD_CHATS} from './actions';
const initialState={

}
export const profileReduser=(state= initialState,action)=>{
    switch(action.type){
        case ADD_CHATS:{
            return(
                
            )
        }
    }
    default:
        return state;
}